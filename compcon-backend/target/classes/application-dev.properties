# =======================================================
# == PERFIL DE DESENVOLVIMENTO (DEV)
# == Usado para rodar a aplica��o localmente
# =======================================================

# Chave secreta para assinar os tokens JWT. DEVE ser uma string longa e aleat�ria.
# Para desenvolvimento, isto � suficiente. Em produ��o, use uma vari�vel de ambiente.
api.security.token.secret=meusupersegredoparadesenvolvimentonaousaremproducao

# --- Conex�o com a Base de Dados (Docker) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/compcon_db
spring.datasource.username=postgres
spring.datasource.password=12345

# --- Configura��es do JPA/Hibernate para Desenvolvimento ---
# 'update': O Hibernate ir� criar/atualizar as tabelas automaticamente. Perfeito para dev.
spring.jpa.hibernate.ddl-auto=update

# Mostra no console os comandos SQL que o JPA est� a executar. �timo para depura��o.
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# --- [A CORRE��O] Desativa TODA a auto-configura��o do Google Cloud ---
# Desativamos a configura��o do SQL e tamb�m a do Core para evitar avisos de credenciais.
spring.cloud.gcp.sql.enabled=false
spring.cloud.gcp.core.enabled=false

# --- [MELHORIA] Desativa o "Open In View" para melhores pr�ticas ---
# Isto resolve o outro aviso (WARN) no seu log e previne problemas de performance.
spring.jpa.open-in-view=false
