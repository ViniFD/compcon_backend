# =======================================================
# == PERFIL DE PRODU��O (PROD)
# == Usado quando a aplica��o estiver a rodar na nuvem (GCP)
# =======================================================

# Chave secreta para assinar os tokens JWT. DEVE ser uma string longa e aleat�ria.
# Para desenvolvimento, isto � suficiente. Em produ��o, use uma vari�vel de ambiente.
api.security.token.secret=meu-super-segredo-para-desenvolvimento-nao-usar-em-producao

# --- Conex�o com a Base de Dados (Cloud SQL) ---
# Esta propriedade especial usa o conector do Google Cloud para uma conex�o segura.
spring.cloud.gcp.sql.instance-connection-name=SEU_PROJETO:SUA_REGIAO:NOME_DA_INSTANCIA_DB
spring.cloud.gcp.sql.database-name=compcon_db
spring.datasource.username=postgres
spring.datasource.password=SUA_SENHA_DA_NUVEM

# --- Configura��es do JPA/Hibernate para Produ��o ---
# 'validate': O Hibernate apenas verifica se as tabelas correspondem �s entidades.
# N�o tenta criar ou alterar nada, o que � mais seguro para produ��o.
spring.jpa.hibernate.ddl-auto=validate

# N�o � recomendado mostrar o SQL no console em produ��o.
spring.jpa.show-sql=false

# Especifica o dialeto SQL para o PostgreSQL.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect